---
id: ci-fix
vars: [repo, mr_iid, mr_title, mr_url, source_branch, target_branch, pipeline_id, pipeline_url, failed_count, failed_jobs, job_logs, branch_diff, checks, worktree, backend_dir, backend_repo]
judge: ci-acceptance
---
Ты работаешь в GitLab-проекте {{repo}}. Текущая директория {{worktree}} — временный git worktree с веткой на базе origin/{{source_branch}}. Основной чекаут проекта не трогай.

Задача: починить упавший пайплайн #{{pipeline_id}} ({{pipeline_url}}) в MR !{{mr_iid}} «{{mr_title}}» ({{mr_url}}), ветка {{source_branch}} → {{target_branch}}.

Упало джоб: {{failed_count}}
{{failed_jobs}}

Логи джоб (выжимка, полный лог — по ссылке на джобу):

{{job_logs}}

Что ветка наделала против {{target_branch}} (`git diff origin/{{target_branch}}...HEAD`, может быть обрезан):

```diff
{{branch_diff}}
```

{{#backend_dir}}
Бэкенд этого проекта: {{backend_dir}} (репозиторий {{backend_repo}}).
Контракт API подтверждай по коду бэкенда: роут → контроллер → Resource/Request → поля и
типы, со ссылкой файл:строка. Сначала прочитай {{backend_dir}}/AGENTS.md. Расхождение
описания задачи с кодом бэкенда называй вслух в отчёте.
{{/backend_dir}}

Шаги:
1. Прочитай логи и дифф выше и найди причину падения в коде ветки. Причина почти всегда в этом диффе: до него джобы были зелёными. Если по выжимке не ясно, посмотри файлы, на которые указывают ошибки, и историю ветки ("git log --oneline -20"); обрезанный дифф добери сам.
2. Почини **причину**. Запрещено: отключать или удалять упавший тест, добавлять skip/only/eslint-disable/ts-ignore, ослаблять правило линтера, менять конфиг CI ради зелёного. Если падение не чинится правкой кода ветки (сломан раннер, чужая зависимость, флейк инфраструктуры) — ничего не коммить и напиши об этом в ответе.
3. Не трогай то, что к падению отношения не имеет: лишние правки судья считает ошибкой.
4. Прогони проверки проекта в этом worktree и добейся зелёного:
{{checks}}
5. Закоммить по правилам проекта: посмотри "git log --oneline -20" и повтори стиль сообщений коммита.
6. НЕ пуши. Push и перезапуск джоб сделает fs-harness сам — после того, как результат посмотрит судья. "git push" в любом виде запрещён, ветку {{target_branch}} не трогай.

В конце ответа кратко: причина каждого падения, что изменено и почему это чинит причину, какие проверки прогнаны и с каким результатом, хэш последнего коммита.
